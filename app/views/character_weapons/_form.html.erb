<%= form_with(model: character_weapon) do |form| %>
  <% if character_weapon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character_weapon.errors.count, "error") %> prohibited this character_weapon from being saved:</h2>

      <ul>
        <% character_weapon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
